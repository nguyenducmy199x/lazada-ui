import { PageProductRequest } from './page-product-request';

describe('PageProductRequest', () => {
  it('should create an instance', () => {
    expect(new PageProductRequest()).toBeTruthy();
  });
});
